# Настройка API сервера Галина

## 🚀 Быстрый старт

1. **Получите API ключ OpenAI:**
   - Перейдите на https://platform.openai.com/api-keys
   - Создайте новый API ключ
   - Скопируйте его

2. **Настройте переменные окружения:**
   ```bash
   cd api/
   # Отредактируйте файл .env
   nano .env
   ```

   Замените:
   ```
   OPENAI_API_KEY=sk-your-actual-openai-api-key-here
   ```
   На ваш реальный API ключ:
   ```
   OPENAI_API_KEY=sk-proj-ваш_реальный_ключ_здесь
   ```

3. **Запустите приложение:**
   ```bash
   cd ..  # вернуться в корневую директорию
   ./start-dev.sh
   ```

## 🔧 Ручная настройка (альтернативный способ)

Если вы используете Docker или другие способы развертывания, установите переменную окружения:

```bash
export OPENAI_API_KEY=sk-proj-ваш_реальный_ключ_здесь
```

## 🧪 Проверка работы

После настройки проверьте работу TTS:

```bash
curl -X POST http://localhost:3003/tts \
  -H "Content-Type: application/json" \
  -d '{"text":"Привет, это тест", "voice":"alloy", "model":"tts-1"}' \
  -o test.mp3

# Проверьте размер файла (должен быть >1000 байт для реального аудио)
ls -la test.mp3
```

## ❓ Проблемы?

Если TTS возвращает файлы размером 48 байт:
- Проверьте, что API ключ правильный
- Убедитесь, что у вас есть баланс на аккаунте OpenAI
- Проверьте логи API сервера: `tail -f api/server_debug.log`

## 🎯 Возможности

После правильной настройки вы получите:
- ✅ Качественный голос от OpenAI TTS
- ✅ Автоматический fallback на браузерный TTS
- ✅ Полноценное голосовое общение
