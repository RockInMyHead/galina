# Настройка базы данных SQLite для Galina API

## 🚀 Быстрый старт

### 1. Создайте .env файл:
```bash
cp .env.production .env
```

### 2. Отредактируйте .env файл:
```env
# OpenAI API Configuration
OPENAI_API_KEY=sk-your-actual-openai-api-key-here

# Server Configuration
NODE_ENV=production
PORT=1041

# Database Configuration (SQLite)
DATABASE_URL="file:./prisma/galina.db"

# JWT Secret (измените на случайный)
JWT_SECRET=your-super-secret-jwt-key-change-this

# CORS Configuration
CORS_ORIGIN=https://lawyer.windexs.ru

# Standalone mode disabled
STANDALONE_MODE=false
```

### 3. Инициализируйте базу данных:
```bash
# Установите зависимости
npm install

# Создайте Prisma клиент
npm run db:generate

# Создайте таблицы в базе данных
npm run db:push

# Создайте демо-пользователя (опционально)
npm run create-demo-user
```

### 4. Запустите сервер:
```bash
npm start
```

## 📊 Структура базы данных

Используется **SQLite** с следующими таблицами:
- `User` - пользователи
- `UserBalance` - балансы пользователей
- `ChatMessage` - сообщения чата
- `File` - загруженные файлы

## 🔧 Управление базой данных

```bash
# Просмотр базы данных в браузере
npm run db:studio

# Создание резервной копии
cp prisma/galina.db prisma/galina.db.backup

# Восстановление из резервной копии
cp prisma/galina.db.backup prisma/galina.db
```

## ⚠️ Важно

- **Standalone режим отключен** - сервер работает с реальной базой данных
- **OPENAI_API_KEY обязателен** для работы чата и TTS
- **База данных создается автоматически** при первом запуске
- **Все данные хранятся локально** в файле `prisma/galina.db`
