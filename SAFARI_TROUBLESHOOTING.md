# Решение проблем с Safari

## Проблема: Safari не может подключиться к http://127.0.0.1:3001

### ✅ Быстрое решение:

1. **Попробуйте localhost вместо 127.0.0.1:**
   ```
   http://localhost:3001
   ```

2. **Очистите кеш Safari:**
   - Safari → Настройки → Дополнения → Разработка
   - Нажмите "Очистить кеш"
   - Или Cmd+Option+E

3. **Проверьте настройки безопасности:**
   - Safari → Настройки → Конфиденциальность
   - Убедитесь, что "Предотвращать отслеживание" не блокирует localhost

4. **Попробуйте режим разработчика:**
   - Safari → Настройки → Дополнения
   - Включите "Показывать меню разработчика"
   - Откройте консоль разработчика (Cmd+Option+C)
   - Проверьте ошибки

### 🔧 Альтернативные решения:

**Вариант 1: Использовать Chrome или Firefox**
```
http://localhost:3001
```

**Вариант 2: Проверить firewall**
```bash
# Проверить, не блокирует ли firewall порт 3001
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
```

**Вариант 3: Перезапустить сервер**
```bash
cd /Users/artembutko/Desktop/Galina
./stop-dev.sh
./start-dev.sh
```

### 🐛 Диагностика:

**Проверить, что сервер запущен:**
```bash
lsof -i :3001
# Должен показать процесс node
```

**Проверить доступность:**
```bash
curl http://127.0.0.1:3001
curl http://localhost:3001
# Оба должны вернуть HTML
```

**Проверить логи сервера:**
- Откройте терминал где запущен `npm run dev`
- Посмотрите на ошибки или предупреждения

### 📱 Рекомендуемые URL для Safari:

1. `http://localhost:3001` (предпочтительно)
2. `http://127.0.0.1:3001` (альтернатива)
3. `http://0.0.0.0:3001` (не рекомендуется)

### 🔒 Если Safari требует HTTPS:

Safari может требовать HTTPS для некоторых функций (например, микрофон). В этом случае:

1. Используйте Chrome или Firefox для разработки
2. Или настройте HTTPS локально (см. `dev_https.sh`)

### ✅ Проверка работы:

После открытия страницы в Safari:
1. Должна загрузиться главная страница Галины
2. В консоли разработчика (Cmd+Option+C) не должно быть ошибок
3. Переход на `/voice` должен работать

### 📞 Если ничего не помогает:

1. Перезагрузите Mac
2. Обновите Safari до последней версии
3. Попробуйте другой браузер для разработки
4. Проверьте, не блокирует ли антивирус/файрвол порт 3001
