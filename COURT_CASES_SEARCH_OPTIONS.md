# Варианты поиска судебных дел без Google/Bing API

## 1. 🆓 Бесплатные поисковые API (без API ключей)

### DuckDuckGo Instant Answer API
- **Бесплатно**: Да, полностью бесплатно
- **API ключ**: Не требуется
- **Лимиты**: Нет строгих лимитов
- **Пример использования**:
```javascript
const searchDuckDuckGo = async (query) => {
  const response = await fetch(`https://api.duckduckgo.com/?q=${encodeURIComponent(query)}&format=json&no_html=1`);
  const data = await response.json();
  return data.RelatedTopics || [];
};
```

### SearxNG (Self-hosted)
- **Бесплатно**: Да, если хостить самостоятельно
- **API ключ**: Не требуется
- **Лимиты**: Зависит от сервера
- **Пример**: Использовать публичные инстансы SearxNG

---

## 2. 🌐 Парсинг сайтов судебных решений напрямую

### Российские сайты с судебными решениями:

#### a) sudrf.ru (Официальный сайт судов РФ)
- **URL**: https://sudrf.ru
- **Метод**: Парсинг HTML
- **Библиотеки**: Cheerio, Puppeteer
- **Сложность**: Средняя (нужно обходить защиту от ботов)

#### b) sudact.ru (Судебные акты РФ)
- **URL**: https://sudact.ru
- **Метод**: Парсинг HTML
- **Библиотеки**: Cheerio, Puppeteer
- **Сложность**: Средняя

#### c) rospravosudie.com (РосПравосудие)
- **URL**: https://rospravosudie.com
- **Метод**: Парсинг HTML
- **Библиотеки**: Cheerio, Puppeteer
- **Сложность**: Средняя

#### d) kad.arbitr.ru (Картотека арбитражных дел)
- **URL**: https://kad.arbitr.ru
- **Метод**: Парсинг HTML или API (если доступен)
- **Библиотеки**: Cheerio, Puppeteer
- **Сложность**: Средняя-Высокая

---

## 3. 🤖 Использование AI для поиска (OpenAI с Web Search)

### OpenAI с функциями поиска
- **Метод**: Использовать OpenAI API с функциями для поиска в интернете
- **Стоимость**: Зависит от использования OpenAI API
- **Пример**: Использовать OpenAI с функциями для поиска судебных дел

---

## 4. 🔍 Специализированные сервисы парсинга

### SerpAPI
- **Стоимость**: Платно, но есть бесплатный тариф
- **API ключ**: Требуется
- **Преимущества**: Парсит Google, Bing, Yahoo без капчи

### ScraperAPI
- **Стоимость**: Платно, есть бесплатный тариф
- **API ключ**: Требуется
- **Преимущества**: Обходит защиту от ботов

### Bright Data (бывший Luminati)
- **Стоимость**: Платно
- **API ключ**: Требуется
- **Преимущества**: Профессиональный парсинг

---

## 5. 📚 Специализированные базы данных судебных решений

### КонсультантПлюс API
- **Стоимость**: Платно
- **API ключ**: Требуется
- **Преимущества**: Официальная база судебных решений

### Гарант API
- **Стоимость**: Платно
- **API ключ**: Требуется
- **Преимущества**: Официальная база судебных решений

### Система ГАС "Правосудие"
- **Стоимость**: Бесплатно (для государственных организаций)
- **API ключ**: Может потребоваться регистрация
- **Преимущества**: Официальная государственная система

---

## 6. 🛠️ Реализация парсинга с помощью библиотек

### Вариант A: Cheerio (для статических сайтов)
```javascript
const cheerio = require('cheerio');
const axios = require('axios');

const parseSudrf = async (query) => {
  const response = await axios.get(`https://sudrf.ru/search?q=${encodeURIComponent(query)}`);
  const $ = cheerio.load(response.data);
  const cases = [];
  
  $('.case-item').each((i, elem) => {
    cases.push({
      title: $(elem).find('.title').text(),
      court: $(elem).find('.court').text(),
      date: $(elem).find('.date').text(),
      url: $(elem).find('a').attr('href'),
      source: 'sudrf.ru'
    });
  });
  
  return cases;
};
```

### Вариант B: Puppeteer (для динамических сайтов)
```javascript
const puppeteer = require('puppeteer');

const parseSudact = async (query) => {
  const browser = await puppeteer.launch({ headless: true });
  const page = await browser.newPage();
  
  await page.goto(`https://sudact.ru/search/?q=${encodeURIComponent(query)}`);
  await page.waitForSelector('.case-item');
  
  const cases = await page.evaluate(() => {
    const items = document.querySelectorAll('.case-item');
    return Array.from(items).map(item => ({
      title: item.querySelector('.title')?.textContent || '',
      court: item.querySelector('.court')?.textContent || '',
      date: item.querySelector('.date')?.textContent || '',
      url: item.querySelector('a')?.href || '',
      source: 'sudact.ru'
    }));
  });
  
  await browser.close();
  return cases;
};
```

---

## 7. 🎯 Рекомендуемый подход (комбинированный)

### Этап 1: Бесплатные API (DuckDuckGo)
- Быстро и бесплатно
- Не требует API ключей
- Хорошо для базового поиска

### Этап 2: Парсинг специализированных сайтов
- sudrf.ru, sudact.ru
- Более точные результаты
- Требует библиотеки для парсинга

### Этап 3: Кэширование результатов
- Сохранять результаты в БД
- Избегать повторных запросов
- Улучшить производительность

---

## 📦 Необходимые зависимости для парсинга

```json
{
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "puppeteer": "^21.0.0"
  }
}
```

---

## ⚠️ Важные замечания

1. **Соблюдение robots.txt**: Всегда проверяйте robots.txt сайта перед парсингом
2. **Rate Limiting**: Не делайте слишком много запросов подряд
3. **User-Agent**: Используйте правильный User-Agent
4. **Юридические аспекты**: Убедитесь, что парсинг разрешен
5. **Защита от ботов**: Некоторые сайты могут блокировать автоматические запросы

---

## 🚀 Быстрый старт

Рекомендуется начать с **DuckDuckGo API** (бесплатно, без API ключей) и затем добавить парсинг специализированных сайтов для более точных результатов.

