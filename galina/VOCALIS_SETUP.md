# Настройка голосового общения (Vocalis)

Этот документ описывает настройку и использование системы голосового общения на основе Vocalis.

## 🎯 Возможности

- **🗣️ Речь-в-речь (Speech-to-Speech)** - Голосовое общение с AI ассистентом
- **🎤 Распознавание речи** - Использует Faster-Whisper для точной транскрипции
- **🔊 Синтез голоса (TTS)** - Генерация естественной речи ассистента
- **⚡ Низкая задержка** - Потоковая обработка аудио для быстрого ответа
- **🔄 Прерывание** - Возможность прервать ассистента во время его речи (barge-in)
- **👋 Автоматическое приветствие** - AI приветствует пользователя при подключении
- **💬 Умное продолжение разговора** - Автоматические follow-up вопросы
- **🖼️ Анализ изображений** - Возможность загрузить и обсудить изображение
- **💾 Управление сессиями** - Сохранение и загрузка разговоров
- **🎨 Визуальная обратная связь** - Анимированная сфера ассистента

## 📋 Требования

### Backend (Python)

Все зависимости уже установлены. Убедитесь, что в `backend/requirements.txt` есть:

```
fastapi==0.109.2
uvicorn==0.27.1
python-dotenv==1.0.1
websockets==12.0
numpy==1.26.4
faster-whisper==1.1.1
requests==2.31.0
python-multipart==0.0.9
torch>=2.0.1
ffmpeg-python==0.2.0
transformers>=4.31.0
```

### Системные требования

- **Python 3.10+**
- **FFmpeg** (дляобработки аудио)
- **LLM сервер** (например, LM Studio, Ollama, или OpenAI API)
- **TTS сервер** (например, Orpheus-FASTAPI, Coqui TTS, или OpenAI TTS)

## 🚀 Настройка

### 1. Установка FFmpeg

#### macOS
```bash
brew install ffmpeg
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

### 2. Настройка LLM сервера

Рекомендуется использовать **LM Studio**:

1. Скачайте и установите [LM Studio](https://lmstudio.ai/)
2. Загрузите модель (например, `llama-3-8b-instruct`)
3. Запустите локальный сервер:
   - В LM Studio откройте вкладку "Local Server"
   - Выберите модель
   - Нажмите "Start Server"
   - Сервер запустится на `http://127.0.0.1:1234`

**Альтернативные варианты:**
- **Ollama**: `ollama serve` (по умолчанию на порту 11434)
- **OpenAI API**: Используйте официальный API

### 3. Настройка TTS сервера

Рекомендуется использовать **Orpheus-FASTAPI**:

1. Клонируйте репозиторий:
```bash
git clone https://github.com/resemble-ai/orpheus-tts
cd orpheus-tts
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите сервер:
```bash
python server.py --port 5005
```

**Альтернативные варианты:**
- **Coqui TTS**: Установите `pip install TTS` и используйте API
- **OpenAI TTS**: Используйте официальный API
- **ElevenLabs**: Подключите через API key

### 4. Настройка backend

Backend уже настроен в `backend/`. Конфигурация находится в `backend/.env` (если файла нет, создайте его):

```bash
# Vocalis backend configuration

# API Endpoints
LLM_API_ENDPOINT=http://127.0.0.1:1234/v1/chat/completions
TTS_API_ENDPOINT=http://localhost:5005/v1/audio/speech

# Whisper Model Configuration
WHISPER_MODEL=base  # Options: tiny.en, base.en, small.en, medium.en, large

# TTS Configuration
TTS_MODEL=tts-1 
TTS_VOICE=tara 
TTS_FORMAT=wav  # Format for TTS output (wav, mp3, opus, flac)

# WebSocket Server Configuration
WEBSOCKET_HOST=0.0.0.0
WEBSOCKET_PORT=8000

# Audio Processing
VAD_THRESHOLD=0.1  # Voice activity detection threshold (0.0-1.0)
VAD_BUFFER_SIZE=30  # Buffer size in milliseconds
AUDIO_SAMPLE_RATE=44100  # Sample rate in Hz
```

### 5. Запуск системы

#### Вариант 1: Использование существующих скриптов

Если у вас есть скрипты `start-project.sh` или `start-dev.sh`, используйте их:

```bash
./start-project.sh
# или
./start-dev.sh
```

#### Вариант 2: Запуск вручную

**Terminal 1 - Backend:**
```bash
cd backend
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Terminal 2 - Frontend:**
```bash
npm run dev
```

## 📱 Использование

1. Откройте браузер и перейдите на страницу голосового общения
2. Нажмите на кнопку микрофона для начала разговора
3. AI автоматически поприветствует вас
4. Говорите - ваша речь будет распознана и обработана
5. AI ответит голосом
6. Вы можете прервать ассистента, начав говорить во время его речи
7. Используйте кнопку "End Call" (красная трубка) для завершения

### Дополнительные функции

- **Загрузка изображения**: Нажмите кнопку с иконкой глаза, выберите изображение и задайте вопрос о нём
- **Сохранение сессии**: Кликните на кнопку меню (слева) и выберите "Save Session"
- **Загрузка сессии**: В меню выберите сохранённую сессию для продолжения разговора
- **Настройки**: В меню можно настроить системный промпт и профиль пользователя

## 🔧 Настройка системного промпта

Системный промпт находится в файле `backend/prompts/system_prompt.md`. Вы можете отредактировать его для изменения поведения ассистента:

```markdown
Вы - полезный, дружелюбный и лаконичный голосовой ассистент.
Отвечайте на запросы пользователей естественным, разговорным образом.
Держите ответы краткими и по делу, так как вы общаетесь голосом.
```

## 🐛 Устранение неполадок

### Backend не запускается

1. Проверьте, установлены ли все зависимости:
```bash
cd backend
pip install -r requirements.txt
```

2. Убедитесь, что Python 3.10+:
```bash
python --version
```

### Ошибка "WebSocket connection failed"

1. Убедитесь, что backend запущен на порту 8000
2. Проверьте в консоли браузера точный адрес WebSocket
3. Проверьте файрвол и антивирус

### TTS не работает

1. Убедитесь, что TTS сервер запущен
2. Проверьте `TTS_API_ENDPOINT` в `.env`
3. Проверьте логи backend для ошибок TTS

### Распознавание речи не работает

1. Проверьте, даны ли разрешения на микрофон в браузере
2. Убедитесь, что FFmpeg установлен
3. Попробуйте изменить `WHISPER_MODEL` на меньший (например, `tiny.en`)

### Медленная обработка

1. Для LLM: Используйте более лёгкую модель или GPU ускорение
2. Для Whisper: Используйте меньшую модель (`tiny.en` или `base.en`)
3. Для TTS: Рассмотрите использование более быстрого TTS движка

## 📚 Дополнительная информация

- [Vocalis GitHub](https://github.com/Lex-au/Vocalis)
- [Faster-Whisper](https://github.com/guillaumekln/faster-whisper)
- [LM Studio](https://lmstudio.ai/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)

## 💡 Советы

1. **Качество микрофона** имеет значение - используйте хороший микрофон для лучшего распознавания
2. **Тихая комната** - минимизируйте фоновый шум
3. **Чёткая речь** - говорите чётко и не слишком быстро
4. **Контекст** - система помнит предыдущий разговор, используйте это
5. **GPU** - если доступен GPU, включите CUDA ускорение для Whisper и LLM

## 🎉 Наслаждайтесь!

Теперь вы готовы использовать голосовое общение с AI! Экспериментируйте с различными настройками и находите оптимальную конфигурацию для ваших нужд.
