# 🚀 Прокси Настройка для Проекта Galina

## 📋 Обзор

Проект Galina настроен для работы через прокси-сервер `185.68.187.20:8000` с аутентификацией `rBD9e6:jZdUnJ`.

Все внешние запросы (OpenAI API, TTS, транскрибация) автоматически идут через прокси.

## 🔧 Конфигурация

### Основные файлы:
- `proxy-config.js` - Конфигурация прокси
- `test-proxy.js` - Скрипт тестирования
- `api/index.js` - API сервер с прокси поддержкой

### Настройки прокси:
```javascript
{
  host: '185.68.187.20',
  port: 8000,
  username: 'rBD9e6',
  password: 'jZdUnJ'
}
```

## 🧪 Тестирование прокси

### Способ 1: Через npm скрипт
```bash
npm run test:proxy
```

### Способ 2: Прямой запуск
```bash
node test-proxy.js
```

### Ожидаемый результат:
```
🧪 Тестирование прокси-сервера...
📡 Прокси: http://rBD9e6:jZdUnJ@185.68.187.20:8000
🎯 Цель: https://httpbin.org/ip
⏳ Отправка запроса через прокси...
✅ Прокси работает!
🌐 Ваш IP через прокси: XXX.XXX.XXX.XXX
```

## 🚀 Запуск проекта

### 1. Установка зависимостей
```bash
npm install
cd api && npm install
```

### 2. Тестирование прокси
```bash
npm run test:proxy
```

### 3. Запуск проекта
```bash
./start-dev.sh
```

## 📡 Как работает прокси

1. **API запросы**: Все запросы к OpenAI API идут через прокси
2. **TTS**: Озвучка через OpenAI TTS идет через прокси
3. **Транскрибация**: Распознавание речи идет через прокси
4. **Локальные запросы**: Запросы к localhost/127.0.0.1 НЕ идут через прокси

## 🔍 Отладка

### Логи прокси:
В консоли API сервера вы увидите:
```
Proxy request: POST https://api.openai.com/v1/chat/completions
Proxy response: 200 https://api.openai.com/v1/chat/completions
```

### Проверка IP:
```bash
curl -x http://rBD9e6:jZdUnJ@185.68.187.20:8000 https://httpbin.org/ip
```

## ⚠️ Важные замечания

1. **Безопасность**: Никогда не коммитьте учетные данные прокси в Git
2. **Производительность**: Прокси может замедлить запросы
3. **Доступность**: Убедитесь, что прокси-сервер доступен
4. **Альтернативы**: Если прокси не работает, можно отключить его в `proxy-config.js`

## 🔧 Изменение настроек прокси

### В proxy-config.js:
```javascript
const proxyConfig = {
  proxy: {
    host: 'ВАШ_ХОСТ',
    port: ВАШ_ПОРТ,
    auth: {
      username: 'ВАШ_ЛОГИН',
      password: 'ВАШ_ПАРОЛЬ'
    }
  }
};
```

### В api/index.js:
```javascript
const proxyUrl = 'http://ВАШ_ЛОГИН:ВАШ_ПАРОЛЬ@ВАШ_ХОСТ:ВАШ_ПОРТ';
```

## 📞 Поддержка

Если прокси не работает:
1. Проверьте доступность сервера
2. Проверьте учетные данные
3. Проверьте сетевые ограничения провайдера
4. Попробуйте другой прокси-сервер
