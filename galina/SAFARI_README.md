# 🧭 Safari Compatibility Guide - Галина AI

## Проблемы с Safari

Safari имеет более строгие политики безопасности, которые могут блокировать подключение к приложению. Вот основные проблемы и решения:

## 🔧 Решения

### 1. **Настройки приватности Safari**
```
Safari → Настройки → Приватность → Отключить:
☐ Предотвращать отслеживание между сайтами
☐ Строгая защита от отслеживания
```

### 2. **Разрешения для localhost**
```
Safari → Настройки → Веб-сайты → Попапы:
☑ Разрешить для localhost
```

### 3. **Очистка кэша**
```
Cmd + Alt + E (Очистить кэш)
Или: Safari → Разработка → Очистить кэш
```

### 4. **Инкогнито режим**
```
Cmd + Shift + N (новое окно инкогнито)
```

### 5. **Проверка CORS**
```
Safari → Разработка → Показать консоль ошибок
Ищите ошибки типа:
- "CORS policy violation"
- "Failed to fetch"
- "Network Error"
```

## 🧪 Тест подключения

Откройте: `http://127.0.0.1:3000/safari-test.html`

Эта страница проверит:
- ✅ Подключение к Vite dev серверу
- ✅ CORS политики
- ✅ Safari-specific compatibility

## 🚀 Запуск приложения

1. **Запустите dev сервер:**
   ```bash
   cd galina
   npx vite --port 3000
   ```

2. **Откройте Safari и перейдите:**
   - `http://127.0.0.1:3000/safari-test.html` (тест)
   - `http://127.0.0.1:3000/` (основное приложение)

3. **Если тест пройден успешно:**
   - Нажмите "Открыть приложение"
   - Или перейдите напрямую на `http://127.0.0.1:3000/`

## 🔄 Альтернативы

Если Safari не работает, используйте:
- **Chrome** - рекомендуемая альтернатива
- **Firefox** - хорошая совместимость
- **Edge** - на базе Chromium

## 📞 Поддержка

Если проблема persists:
1. Откройте Safari → Разработка → Показать консоль ошибок
2. Скопируйте ошибки и создайте issue
3. Попробуйте Chrome для сравнения

## ⚙️ Технические детали

Приложение настроено для Safari совместимости:
- `host: '127.0.0.1'` (вместо localhost)
- `hmr: false` (отключен Hot Module Replacement)
- CORS заголовки для локального развития
- Оптимизированные заголовки безопасности