# 🎤 Настройка голосового распознавания

## ⚡ Быстрый старт

**Для тестирования без настройки HTTPS:**
```bash
npm run dev -- --port 3002
# Откройте http://localhost:3002/voice
# Тестовый режим включится автоматически
```

**Для полной функциональности с микрофоном:**
```bash
./dev_https.sh
# Откройте https://localhost:3002/voice
```

## Проблема

Web Speech API требует **HTTPS соединения** для работы с микрофоном. На localhost по умолчанию используется HTTP, что вызывает ошибку "Failed to access assets".

## Решения

### 🚀 Решение 1: Настройка HTTPS для разработки (Рекомендуется)

#### Шаг 1: Установка mkcert
```bash
# macOS
brew install mkcert
mkcert -install

# Linux
sudo apt install mkcert
mkcert -install

# Windows (Chocolatey)
choco install mkcert
mkcert -install
```

#### Шаг 2: Создание сертификатов
```bash
# Из корневой папки проекта
./setup_https.sh
```

#### Шаг 3: Настройка Vite
Добавьте в `vite.config.ts`:
```typescript
export default defineConfig({
  server: {
    host: "::",
    port: 0,
    https: {
      key: './localhost-key.pem',
      cert: './localhost.pem'
    }
  },
  // ... остальные настройки
});
```

#### Шаг 4: Запуск с HTTPS
```bash
npm run dev -- --port 3002
```

#### Шаг 5: Откройте приложение
```
https://localhost:3002
```

### 🔧 Решение 2: Настройка браузера (Быстрый способ)

#### Chrome
1. Откройте `chrome://flags/#unsafely-treat-insecure-origin-as-secure`
2. Добавьте `http://localhost:3002` в список
3. Перезапустите браузер

#### Firefox
1. Откройте `about:config`
2. Найдите `media.webspeech.recognition.force_allow_insecure`
3. Установите значение `true`
4. Перезапустите браузер

### 🧪 Решение 3: Тестовый режим (Для разработки)

Если ничего не помогает, используйте встроенный тестовый режим:

1. Откройте приложение на `http://localhost:3002`
2. При ошибке появится желтая панель "Режим разработки"
3. Вводите текст вручную или используйте кнопку "🎲 Случайный"
4. Функциональность AI полностью работает

## Диагностика

### Проверка логов

При запуске приложения проверяйте консоль браузера (F12):

```
✅ Web Speech API supported, creating recognition instance...
🎙️ Requesting microphone permission...
✅ Microphone permission granted, testing audio context...
🎤 Speech recognition started successfully
```

### Возможные ошибки

- **"Failed to access assets"** → Требуется HTTPS
- **"not-allowed"** → Разрешите доступ к микрофону
- **"network"** → Проверьте интернет соединение
- **"no-speech"** → Говорите громче или ближе к микрофону

## Тестирование

### Простой тест
```bash
# Откройте test_speech_recognition.html в браузере
open test_speech_recognition.html
```

### Полный тест
1. Запустите приложение с HTTPS
2. Откройте `/voice`
3. Нажмите "Начать непрерывное прослушивание"
4. Говорите: "Привет, как дела?"
5. Проверьте, что текст появляется и отправляется

## Советы

- 🎤 Используйте хороший микрофон
- 🔊 Говорите четко и не слишком быстро
- 🌐 Для production HTTPS обязателен
- 🧪 Тестовый режим всегда доступен для разработки

## Статус

- ✅ Тестовый режим работает
- ✅ HTTPS настройка готова
- ✅ Диагностика улучшена
- ✅ Совместимость с браузерами
